# src/ is intentionally a placeholder

Each instrument lives in its own folder under `variants/`, selected by the
matching PlatformIO `[env:...]` via `build_src_filter`. Shared code lives in `lib/`.
See the top-level README for the variant matrix.
